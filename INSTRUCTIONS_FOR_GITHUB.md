# طريقة نشر موقعك على GitHub (خطوة بخطوة)

أهلاً بك! هذا الدليل سيساعدك على رفع شيفرة موقعك إلى GitHub بسهولة. GitHub هو موقع لحفظ نسخ من شيفرة مشاريعك ومشاركتها.

---

### الخطوة الأولى: إنشاء حساب ومستودع على GitHub

1.  **اذهب إلى موقع [GitHub](https://github.com).** إذا لم يكن لديك حساب، قم بإنشاء حساب جديد (مجاني).
2.  بعد تسجيل الدخول، اضغط على علامة `+` في الزاوية العلوية اليمنى، ثم اختر **New repository** (مستودع جديد).
3.  **املأ التفاصيل:**
    *   **Repository name (اسم المستودع):** اختر اسمًا لمشروعك، مثلاً: `susan-fashion-store`.
    *   **Description (الوصف):** هذا اختياري، يمكنك كتابة "متجر أزياء سوزان".
    *   اختر **Public** (عام) حتى يتمكن أي شخص من رؤية الموقع.
4.  اضغط على زر **Create repository** (إنشاء مستودع).

---

### الخطوة الثانية: تجهيز حاسوبك

لرفع الملفات، ستحتاج إلى برنامجين على حاسوبك: `Git` و `GitHub CLI`.

1.  **ثبّت `Git`:** اذهب إلى [هذا الرابط](https://git-scm.com/downloads) وقم بتحميل وتثبيت النسخة المناسبة لحاسوبك (Windows, Mac, أو Linux).
2.  **ثبّت `GitHub CLI`:** اذهب إلى [هذا الرابط](https://cli.github.com) وقم بتحميل وتثبيت الأداة. هذه الأداة ستسهل عليك تسجيل الدخول.

---

### الخطوة الثالثة: رفع ملفات المشروع

1.  **قم بتحميل ملفات مشروعك:** في بيئة العمل التي تتحدث معي فيها الآن، ابحث عن زر لتحميل المشروع كملف مضغوط (`.zip`).
2.  **فك ضغط الملف:** بعد التحميل، فك ضغط الملف في مجلد على حاسوبك، مثلاً على سطح المكتب.
3.  **افتح الطرفية (Terminal):**
    *   **في ويندوز:** ابحث عن `Command Prompt` أو `PowerShell` وافتحه.
    *   **في ماك أو لينكس:** ابحث عن `Terminal` وافتحه.
4.  **اذهب إلى مجلد مشروعك:** في الطرفية، اكتب الأمر `cd` ثم مسافة، واسحب المجلد الذي فكيت ضغطه إلى نافذة الطرفية. سيبدو الأمر هكذا واضغط Enter:
    ```bash
    cd C:\Users\YourUser\Desktop\susan-fashion-store
    ```
5.  **سجل دخولك إلى GitHub:** اكتب الأمر التالي في الطرفية واضغط Enter. سيطلب منك تسجيل الدخول عبر المتصفح.
    ```bash
    gh auth login
    ```
---

### الخطوة الرابعة والأخيرة: تشغيل أوامر الرفع

انسخ الأوامر التالية **واحدًا تلو الآخر** والصقها في الطرفية، واضغط `Enter` بعد كل أمر. **مهم:** استبدل `YOUR_USERNAME` و `YOUR_REPOSITORY_NAME` باسم المستخدم واسم المستودع الخاص بك على GitHub.

1.  **تهيئة المجلد (يُنفذ مرة واحدة فقط):**
    ```bash
    git init -b main
    ```
2.  **ربط مجلدك بمستودع GitHub:**
    ```bash
    git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY_NAME.git
    ```
3.  **إضافة جميع الملفات للرفع:**
    ```bash
    git add .
    ```
4.  **حفظ نسخة من عملك مع رسالة:**
    ```bash
    git commit -m "تم رفع أول نسخة من المتجر"
    ```
5.  **رفع الملفات إلى GitHub:**
    ```bash
    git push -u origin main
    ```

**مبروك!** بعد انتهاء الأمر الأخير، إذا ذهبت إلى صفحة المستودع على موقع GitHub، ستجد جميع ملفات مشروعك هناك.
